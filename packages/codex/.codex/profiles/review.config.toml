# Codex profile: review
# レビュー専用 (read-only)。gpt-5.5 を使用。書き込み不可。
# 注: gpt-5.3-codex は ChatGPT sign-in で deprecated のため不使用。
# 詳細: ~/.claude/rules/code-review.md, ~/.claude/rules/document-review.md
model = "gpt-5.5"
approval_policy = "never"
sandbox_mode = "read-only"
