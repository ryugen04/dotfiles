# macOS固有設定

# macOS のウィンドウタイトルバーに kitty のタブタイトルを反映しない。
# Claude Code hook が set-tab-title でタブ名を動的に書き換えるため、
# これを許すと macOS 画面上部のウィンドウ名まで書き換わってしまう。
# タブ表示は kitty 自身の tab_bar で完結させる。
macos_show_window_title_in none
