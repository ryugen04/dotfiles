# Claude Code MCP Servers
# インストール: ~/.claude/scripts/install-mcp.sh

servers:
  # Gemini CLI経由でGeminiを使用（OAuth認証）
  - name: gemini-cli
    command: npx
    args:
      - "-y"
      - "gemini-mcp-tool"

  - name: context7
    command: npx
    args:
      - "-y"
      - "@upstash/context7-mcp"

  # GitHub - 環境変数: GITHUB_PERSONAL_ACCESS_TOKEN
  # - name: github
  #   command: npx
  #   args:
  #     - "-y"
  #     - "@modelcontextprotocol/server-github"
  #   env:
  #     GITHUB_PERSONAL_ACCESS_TOKEN: "${GITHUB_PERSONAL_ACCESS_TOKEN}"

  # Slack - 環境変数: SLACK_BOT_TOKEN, SLACK_TEAM_ID, SLACK_CHANNEL_IDS
  # - name: slack
  #   command: npx
  #   args:
  #     - "-y"
  #     - "@modelcontextprotocol/server-slack"
  #   env:
  #     SLACK_BOT_TOKEN: "${SLACK_BOT_TOKEN}"
  #     SLACK_TEAM_ID: "${SLACK_TEAM_ID}"
  #     SLACK_CHANNEL_IDS: "${SLACK_CHANNEL_IDS}"
