---
description: Generate a Markdown diagram document with Mermaid visualizations
skill: visual-explain
---
Generate a Markdown diagram for: $@

**Output must be written in Japanese.**

Follow the visual-explain skill workflow. Read the reference files (`./references/libraries.md` for Mermaid syntax, `./references/markdown-patterns.md` for formatting patterns) before generating. Pick a structure that fits the content.

Write to `~/.agent/diagrams/` with a descriptive filename and tell the user the file path.
